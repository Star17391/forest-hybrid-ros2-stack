# Parâmetros do nó `camera` (usb_cam)
camera:
  ros__parameters:
    video_device: "/dev/video0"
    framerate: 30.0
    io_method: "mmap"
    frame_id: "camera_optical_frame"
    pixel_format: "mjpeg2rgb"
    image_width: 640
    image_height: 480
    camera_name: "camera"
    camera_info_url: ""
